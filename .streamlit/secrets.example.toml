# Example secrets file for Streamlit Cloud
# Copy this to .streamlit/secrets.toml and fill in actual values
# Note: secrets.toml is in .gitignore and won't be committed

# If you need to add any API keys or configuration:
# my_api_key = "your-api-key-here"
